# Core
ollama>=0.4.0

# NLP model (optional — fallback to Ollama if not installed)
# transformers>=4.40.0
# torch>=2.0.0

# MongoDB backend (optional)
# pymongo>=4.0.0

# Neo4j backend (optional)
# neo4j>=5.0.0
